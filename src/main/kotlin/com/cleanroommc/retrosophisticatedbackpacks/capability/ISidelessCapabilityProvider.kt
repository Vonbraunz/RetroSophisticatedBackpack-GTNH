package com.cleanroommc.retrosophisticatedbackpacks.capability

// Marker interface — capability dispatch is not available in 1.7.10.
// Upgrade type dispatch happens via BackpackWrapper.gatherUpgrades<T>() (reified inline).
interface ISidelessCapabilityProvider
